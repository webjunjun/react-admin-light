import { Layout } from 'antd'
const { Header } = Layout

const LayoutHeader = () => {
  return (
    <Header
      className="site-layout-background"
      style={{
        padding: 0
      }}
    />
  )
}

export default LayoutHeader
